# Portfolio Trouble Shooting Notes

## 방향성

- 현재 정리는 문제 인식은 좋지만, 일부 표현이 추상적이고 "아이디어 제안"과 "코드로 구현한 해결"이 섞여 있다.
- 면접에서는 `문제 -> 원인 -> 내가 실제로 바꾼 것 -> 검증 방식 -> 결과` 순서로 말해야 방어가 쉽다.
- "문의 감소", "신뢰 확보", "경쟁력 확보" 같은 결과 표현은 좋지만, 가능하면 수치나 관찰 기준을 함께 붙이는 편이 강하다.
- "크로스 플랫폼 니즈 충족", "안정적 동작 확보"처럼 넓은 표현은 꼬리 질문이 붙기 쉽다. 어떤 범위까지 검증했는지 선을 명확히 그어두는 게 안전하다.

## 전체 첨삭 기준

- `문제`는 현상 중심으로 쓴다.
- `원인`은 기술 원인과 운영 원인을 구분한다.
- `해결`은 내가 직접 구현하거나 설계한 변경 사항만 쓴다.
- `결과`는 가능하면 수치, 없으면 관찰 가능한 변화로 쓴다.
- "가능성을 검증"과 "실서비스 수준으로 제공"은 다른 말이다. MVP 수준이면 그렇게 제한해서 말한다.
- "개선", "안정화", "고도화"는 자주 쓰면 약해진다. 무엇을 어떻게 바꿨는지 구체 동사로 바꾼다.

## 면접 기준으로 더 강한 문장 구조

다음 형식으로 말하면 꼬리 질문에 버티기 좋다.

1. 고객사나 운영 현장에서 어떤 문제가 반복됐는지 설명
2. 로그, 문의 패턴, 디바이스 특성, SDK 구조 중 무엇이 원인이었는지 분해
3. 내가 바꾼 인터페이스, 라이프사이클 구조, 예외 처리, 알고리즘, 테스트 방식 설명
4. 적용 후 문의량, 크래시, 인식률, 도입 여부 등 어떤 변화가 있었는지 설명
5. 한계와 후속 과제까지 짧게 덧붙임

---

## 1. SDK / 플랫폼 연동

### A안. 크로스 플랫폼 지원 부족 이슈

#### 기존 표현의 약점

- "크로스 플랫폼 연동 가능성 검증"은 좋지만, 실제로 무엇을 만들었는지 보이지 않는다.
- "현장 기술 지원까지 제공"은 본인 강점이지만, 코드 레벨 기여가 약하게 들릴 수 있다.
- 꼬리 질문:
  - Flutter, React Native, Cordova 각각 어떤 방식으로 붙였는가?
  - MethodChannel인지, 브리지 모듈인지, AAR wrapping인지?
  - 어디까지 지원했고, 어디까지는 고객사 구현이었는가?

#### 추천 문안

- 문제: SDK가 Android Native 앱 중심으로 설계되어 있어, Flutter·React Native·Cordova 기반 고객사는 도입 검토 자체가 어려웠다.
- 원인: SDK 공개 인터페이스가 Native 사용 전제를 깔고 있었고, 멀티플랫폼 브리지 예제와 연동 가이드가 없었다.
- 해결: 우선순위가 높은 기능만 추려 MVP 브리지 샘플을 빠르게 구현하고, 플랫폼별 연동 포인트와 초기화/권한 처리/결과 전달 방식을 문서화했다. 이후 고객사 환경에서 직접 연동을 지원하며 적용 가능 범위를 검증했다.
- 결과: 크로스 플랫폼 고객사의 기술 검토 장벽을 낮췄고, 실제 계약 및 신규 매출로 연결됐다.

#### 더 좋게 말하는 포인트

- "모든 플랫폼을 완전 지원"처럼 말하지 말고, "핵심 기능 기준으로 연동 가능성을 검증했다"고 말하는 편이 안전하다.
- 실제로 구현한 브리지 방식이 있으면 반드시 언급한다.
  - 예: `MethodChannel로 SDK init/detect API 노출`
  - 예: `성공/실패 콜백을 JSON payload로 변환해 상위 레이어에 전달`

### B안. SDK 적용 결과 확인 불가 이슈

#### 기존 표현의 강점

- 문제와 원인이 명확하다.
- "람다 기반 성공/실패 콜백", "Enum 기반 에러 코드"는 코드 레벨 설명이 가능하다.

#### 보완 포인트

- 왜 `람다 기반`이었는지 이유를 붙이면 더 좋다.
- 에러 코드를 어떤 기준으로 분류했는지 설명 가능해야 한다.

#### 추천 문안

- 문제: 고객사가 SDK를 적용한 뒤에도 호출 성공 여부와 실패 원인을 바로 확인할 수 없어 기술 문의가 반복됐다.
- 원인: 기존 SDK 인터페이스는 실행 결과를 구조적으로 반환하지 않았고, 실패 상황도 공통 에러 체계 없이 흩어져 있었다.
- 해결: SDK 호출 결과를 성공/실패 콜백으로 명시적으로 분리하고, 주요 실패 케이스를 Enum 기반 에러 코드로 표준화했다. 또한 고객사가 바로 대응할 수 있도록 에러 코드별 원인과 조치 방법을 문서화했다.
- 결과: 고객사에서 자체적으로 원인 파악이 가능해지면서 문의량이 줄었고, 연동 및 운영 속도도 개선됐다.

#### 면접 꼬리 질문 대비

- 에러 코드는 어떤 기준으로 나눴나?
  - 권한, 입력값, 네트워크, 라이프사이클, 내부 처리 실패 등으로 계층화했다고 답할 수 있어야 한다.
- 왜 Exception 그대로 던지지 않았나?
  - SDK 소비자 입장에서는 예외 메시지보다 안정적인 계약이 더 중요하고, 다국어/문서화/분기 처리도 에러 코드가 더 유리하다고 설명하면 된다.

### C안. Camera Lifecycle 관리 미비로 인한 Crash

#### 기존 표현의 강점

- 고객사 앱 환경이 다양하다는 점을 잘 짚었다.
- "Lifecycle 주입"은 설계 개선으로 들린다.

#### 보완 포인트

- 어떤 크래시였는지 한 단계만 더 구체화하는 편이 낫다.
- 예: 중복 open/close, paused 상태 접근, surface 해제 이후 접근 등

#### 추천 문안

- 문제: 고객사 앱에서 화면 전환이나 백그라운드 진입 시점에 Camera를 직접 제어하는 과정에서 크래시가 반복 발생했다.
- 원인: 앱마다 Lifecycle 흐름이 달라 Camera open/close 타이밍을 수동으로 맞추기 어려웠고, 잘못된 시점의 접근이 누적되며 충돌이 발생했다.
- 해결: SDK가 LifecycleOwner를 주입받아 카메라 리소스를 생명주기에 맞춰 자동 관리하도록 구조를 바꿨다. 동시에 특수한 화면 구조를 가진 앱을 위해 수동 제어 옵션도 별도로 제공했다.
- 결과: 고객사 환경별 예외 케이스를 줄였고, 카메라 관련 크래시를 유의미하게 감소시켰다.

#### 꼬리 질문 대비

- 자동 관리와 수동 관리가 충돌하지 않게 어떻게 분리했나?
- onStart/onResume/onPause/onStop 중 어느 시점에 어떤 리소스를 정리했나?
- Surface, CameraDevice, CaptureSession 중 어떤 자원을 언제 닫았나?

---

## 2. Tracer / 검출 기능

### A안. 촬영 거리/초점 미인지로 인한 인식률 저하

#### 기존 표현의 약점

- "안정적인 인식률 확보"가 조금 넓다.
- 자동 반복 줌이 왜 필요한지 알고리즘 관점 설명이 있으면 더 좋아진다.

#### 추천 문안

- 문제: 고객사가 촬영 거리와 초점이 검출 성능에 미치는 영향을 인지하지 못해, 동일한 기능도 현장별 인식률 편차가 컸다.
- 원인: 수동 초점 조정이 익숙하지 않은 사용자가 많았고, 피사체 거리 변화에 따라 최적 배율과 초점 지점이 달라졌기 때문이다.
- 해결: Auto Focus를 기본 적용하고, 1배~3배 구간을 자동으로 탐색하는 줌 루프를 추가해 사용자가 최적 지점을 수동으로 찾지 않아도 되도록 했다.
- 결과: 사용자 조작 부담을 줄이면서 검출 성공률을 보다 안정적으로 확보할 수 있었다.

#### 꼬리 질문 대비

- 왜 1~3배였는가?
- 고정 줌이 아니라 반복 탐색으로 간 이유는?
- 속도와 정확도 trade-off는 어떻게 봤는가?

### B안. 왜곡/측면 이미지 검출 실패

#### 기존 표현의 강점

- 문제와 원인이 기술적으로 자연스럽다.
- Perspective, Resize, Crop은 구현 요소라 설명하기 좋다.

#### 보완 포인트

- "포토샵 유사"는 빼는 편이 낫다. 면접에서는 제품 비유보다 처리 파이프라인이 더 강하다.

#### 추천 문안

- 문제: 정면 촬영이 아닌 왜곡 이미지나 측면 촬영 이미지는 단순 프레임 입력만으로는 검출 실패가 잦았다.
- 원인: 워터마크가 포함된 영역이 원근 왜곡, 기울어짐, 비율 변화의 영향을 받으면서 검출 알고리즘이 기대하는 입력 형태에서 벗어났기 때문이다.
- 해결: 입력 이미지에 대해 Perspective 보정, 리사이징, Crop 등 전처리 파이프라인을 제공해 검출 가능한 형태로 정규화했다.
- 결과: 왜곡되거나 측면에서 촬영된 이미지에서도 검출 성공 사례를 확보했고, 현장 활용 범위를 넓혔다.

#### 꼬리 질문 대비

- 전처리 순서는 왜 그 순서였나?
- 보정 비용 때문에 속도 저하는 없었나?
- OpenCV를 썼는지, 직접 행렬 계산을 했는지?

### C안. 내부망 환경 로그인 불가

#### 기존 표현의 약점

- "암호화된 파일 기반 로그인"은 흥미롭지만, 보안 질문이 강하게 붙는다.
- 인증/인가를 대체한 것인지, 오프라인 토큰인지 선을 분명히 해야 한다.

#### 추천 문안

- 문제: 외부망 접속이 제한된 내부망 고객사는 서버 기반 로그인 구조 때문에 서비스 도입이 어려웠다.
- 원인: 기존 인증 흐름이 온라인 서버 검증을 전제로 설계되어 폐쇄망 환경을 지원하지 못했다.
- 해결: 고객사 운영 환경을 고려해 암호화된 파일 기반 인증 정보를 사용할 수 있는 오프라인 인증 방식을 추가했다. 파일 생성과 배포, 검증 규칙을 별도로 정의해 내부망에서도 동일한 서비스 흐름을 유지할 수 있게 했다.
- 결과: 폐쇄망 환경 고객사에도 서비스를 제공할 수 있게 되었고, 경쟁 제품 대비 도입 장벽을 낮췄다.

#### 꼬리 질문 대비

- 위변조 방지는 어떻게 했나?
- 만료나 회수는 어떻게 처리했나?
- 단순 텍스트 파일과 무엇이 달랐나?

---

## 3. 카메라 성능 연구

### A안. FPS 변동으로 인한 인식률 편차

#### 기존 표현의 강점

- 원인 설명이 기술적으로 설득력 있다.
- 단순 현상 정리가 아니라 실험 방향이 보인다.

#### 보완 포인트

- "결론을 도출"보다 "어떤 조건에서 유의미했다"가 더 강하다.
- Flash와 강제 FPS 고정의 목적이 다르면 분리해서 말하는 편이 좋다.

#### 추천 문안

- 문제: 조도와 피사체 움직임에 따라 카메라 FPS가 흔들리면서 동일한 알고리즘도 인식률 편차가 크게 발생했다.
- 원인: Camera2 API의 자동 노출/자동 FPS 제어 특성상 저조도 환경에서는 셔터 시간이 길어지고, 그에 따라 실제 FPS가 낮아졌기 때문이다.
- 해결: 저조도 환경에서 Flash 사용 시 FPS가 얼마나 안정화되는지 먼저 검증했고, 이후 강제 FPS 고정 설정을 적용해 인식률 개선 효과와 CPU·메모리·발열 영향을 함께 측정했다.
- 결과: FPS 안정화가 특정 환경에서 인식률 개선에 직접적인 영향을 준다는 점을 확인했고, 이후 SDK 카메라 설정 최적화 방향을 정하는 근거로 활용했다.

#### 꼬리 질문 대비

- 실험 환경을 어떻게 통제했나?
- 어떤 디바이스에서 유의미했나?
- FPS를 올리면 오히려 노이즈가 늘거나 발열이 커지지 않았나?

### B안. 데이터 수집 신뢰도 부족

#### 기존 표현의 강점

- 연구다운 문제 설정이다.
- "좌표계, 수평계, 자동 송출"은 구현 포인트가 있다.

#### 추천 문안

- 문제: 초기 실험은 디바이스 위치와 각도 편차가 커서 반복 측정 시 데이터 신뢰도가 낮았다.
- 원인: 촬영 위치와 자세를 사람이 수동으로 맞추는 방식이라 조건 재현성이 떨어졌기 때문이다.
- 해결: 테스트 앱에 좌표 가이드, 수평계, 측정 결과 자동 저장/송출 기능을 추가해 실험 조건을 표준화했다.
- 결과: 반복 측정에서도 비교 가능한 데이터를 안정적으로 확보할 수 있었고, 디바이스별 성능 차이를 더 신뢰성 있게 분석할 수 있었다.

#### 꼬리 질문 대비

- 어떤 값들을 자동 저장했나?
- 동일 조건 여부를 어떤 기준으로 판단했나?

### C안. 인식률 정의의 모호성

#### 기존 표현의 강점

- 이건 면접에서 좋다. 단순 구현이 아니라 "평가 기준 수립" 경험으로 들린다.

#### 보완 포인트

- "절대적/상대적 인식률"의 정의를 한 문장으로 준비해두는 게 필수다.

#### 추천 문안

- 문제: 기존에는 성공/실패만 집계해 인식률을 평가했기 때문에, 디바이스나 환경 변화에 따른 성능 차이를 정교하게 비교하기 어려웠다.
- 원인: 인식률 계산식과 측정 단위가 명확하지 않았고, 측정 과정도 사람의 판단에 많이 의존했다.
- 해결: 총 처리 프레임 수, 측정 시간, 성공 검출 횟수, 평균 FPS 등을 함께 반영하는 지표를 정의하고, 절대 성능과 상대 비교가 가능하도록 평가 기준을 분리했다.
- 결과: 디바이스별·환경별 성능 비교가 가능한 평가 체계를 만들었고, 최적화 방향을 감이 아니라 데이터로 판단할 수 있게 됐다.

#### 꼬리 질문 대비

- 절대 인식률과 상대 인식률의 차이는?
- 실무 의사결정에는 어떤 지표가 더 유용했나?

---

## 4. KEEFO

### A안. 디바이스 해상도/밀도 차이로 인한 티켓 품질 저하

#### 기존 표현의 강점

- 현장 피드백 반영 사례라 실무성이 좋다.
- Down/Up Sampling, Crop, 분기 처리 모두 기술 포인트가 있다.

#### 보완 포인트

- "더 긴 곳" 같은 표현은 면접에서는 "긴 축(long edge)"처럼 정리하는 편이 낫다.

#### 추천 문안

- 문제: 디바이스별 해상도와 화면 밀도 차이로 인해 온라인 티켓 UI가 깨지거나, 티켓 스캔 인식률이 떨어지는 문제가 발생했다.
- 원인: 고정 크기 기준으로 티켓을 생성하다 보니 다양한 디바이스의 화면 비율과 밀도를 반영하지 못했다.
- 해결: 디바이스의 긴 축과 밀도를 기준으로 티켓 크기를 동적으로 조정하고, Up/Down Sampling 및 최종 Crop을 적용해 워터마크 품질과 UI 가독성을 동시에 맞췄다.
- 결과: 다양한 디바이스에서 스캔 가능한 티켓을 안정적으로 출력할 수 있었고, 화면 품질 저하도 줄일 수 있었다.

#### 꼬리 질문 대비

- 어떤 해상도 구간에서 분기했나?
- UI 품질과 검출률 중 충돌이 있었나?

### B안. 티켓 확인 프로세스 복잡도

#### 기존 표현의 강점

- 사용자 경험 개선 사례다.
- 현장 운영 관점이 살아 있다.

#### 추천 문안

- 문제: 일부 사용자가 발급된 티켓 위치를 바로 찾지 못해 입장 과정이 지연됐다.
- 원인: 티켓 확인 경로가 직관적이지 않았고, 운영 현장에서 사전 안내도 충분하지 않았다.
- 해결: 흔들기(Shake) 제스처로 티켓 화면에 바로 진입할 수 있게 해 탐색 단계를 줄였고, 현장 안내 문구도 함께 보완했다.
- 결과: 티켓 확인 시간이 단축됐고, 입장 현장에서의 반복 문의도 줄었다.

#### 꼬리 질문 대비

- 오작동은 없었나?
- 접근성을 해치지는 않았나?

### C안. 보안 필름/파손 디바이스에서 워터마크 인식 실패

#### 기존 표현의 약점

- 이건 앱 코드로 해결했다기보다 운영 프로세스로 예외 처리한 사례다.
- 나쁘진 않지만, "기술적 해결"로 밀기엔 약하다.

#### 추천 문안

- 문제: 보안 필름이 부착되었거나 액정이 손상된 일부 디바이스에서는 워터마크 인식 실패가 발생했다.
- 원인: 모아레 현상이나 화면 손상으로 인해 워터마크 패턴이 정상적으로 카메라에 입력되지 않았기 때문이다.
- 해결: 기술적으로 완전한 보정이 어려운 예외 케이스로 판단해, 운영 측면에서 Admin Page에 티켓 번호 수기 입력 기능을 제안하고 반영했다.
- 결과: 예외 상황에서도 입장 프로세스를 중단하지 않도록 운영 안정성을 확보했다.

#### 말할 때 주의

- 이 사례는 "기술적으로 끝까지 밀어붙였다"보다 "현실적인 운영 대안을 설계했다"로 포지셔닝하는 편이 맞다.

---

## 코드 레벨로 더 쓰기 좋은 사례

현재 작업 중인 `android-common_sdk` 저장소 기준으로는 아래 주제가 코드 관점 설명에 더 적합하다.

### 1. JNI 기반 SDK 배포 안정화

- 배경: SDK 핵심 기능이 네이티브 라이브러리(`STNDK`)와 OpenCV 의존성을 포함하고 있어, 고객사 앱에 따라 AAR 통합 시 ABI 누락이나 네이티브 로딩 이슈가 발생할 수 있었다.
- 내가 한 일:
  - AAR에 필요한 `.so` 라이브러리를 ABI별로 포함하도록 정리했다.
  - `jniLibs` 경로를 명시해 배포 산출물에 네이티브 라이브러리가 누락되지 않도록 구성했다.
  - 라이브러리 로딩 지점을 단일화해 SDK 초기 구간에서 문제를 빠르게 드러나게 했다.
- 근거 코드:
  - [`STNative.kt`](/Users/dongjun-yeong/Documents/DevCenter/android-common_sdk/app/src/main/java/com/snaptag/stnative/STNative.kt#L3) 의 `System.loadLibrary("STNDK")`
  - [`app/build.gradle`](/Users/dongjun-yeong/Documents/DevCenter/android-common_sdk/app/build.gradle#L60) 의 `jniLibs.srcDir`
- 면접용 한 줄:
  - "네이티브 검출 엔진을 AAR로 배포할 때 ABI별 `.so` 누락과 로딩 실패를 막기 위해 배포 구성을 정리하고, SDK 로딩 경로를 단일화했습니다."

### 2. AAR 배포 버전 관리 및 소비자 통합 편의 개선

- 배경: SDK를 고객사 프로젝트에 반복 배포하다 보면, 버전 식별이 불명확하거나 debug/release 산출물 관리가 꼬이기 쉽다.
- 내가 한 일:
  - Maven publication을 분리하고, 산출물 이름을 버전 기반으로 고정해 배포 이력을 추적하기 쉽게 만들었다.
- 근거 코드:
  - [`app/build.gradle`](/Users/dongjun-yeong/Documents/DevCenter/android-common_sdk/app/build.gradle#L12)
  - [`app/build.gradle`](/Users/dongjun-yeong/Documents/DevCenter/android-common_sdk/app/build.gradle#L52)
- 면접용 한 줄:
  - "고객사 전달용 AAR의 버전 식별성과 재배포 안정성을 높이기 위해 publication과 산출물 네이밍 규칙을 정리했습니다."

### 3. SDK API 표면 최소화 및 Native 브리지 캡슐화

- 배경: 네이티브 연동 코드가 앱 전역에 흩어지면 유지보수와 디버깅이 어려워진다.
- 내가 한 일:
  - 네이티브 함수 호출을 단일 객체로 감싸서 브리지 레이어를 분리하고, 외부에서는 핵심 detect/embed 기능 중심으로 접근하도록 구조화했다.
- 근거 코드:
  - [`STNative.kt`](/Users/dongjun-yeong/Documents/DevCenter/android-common_sdk/app/src/main/java/com/snaptag/stnative/STNative.kt#L3)
- 면접용 한 줄:
  - "JNI 호출부를 별도 객체로 캡슐화해 Native 의존성을 한 곳으로 모으고, 이후 에러 처리나 API 변경 영향 범위를 줄였습니다."

### 4. 릴리즈 빌드 난독화 환경에서 SDK 동작 보장

- 배경: 라이브러리 SDK는 고객사 앱의 release/minify 환경에서만 드러나는 문제가 있다.
- 내가 한 일:
  - release 빌드에서도 Proguard/minify 기준으로 검증 가능한 배포 구성을 유지하고, 소비자 앱의 릴리즈 환경을 전제로 SDK 동작을 확인하는 흐름을 만들었다.
- 근거 코드:
  - [`app/build.gradle`](/Users/dongjun-yeong/Documents/DevCenter/android-common_sdk/app/build.gradle#L84)
- 면접용 한 줄:
  - "SDK는 debug에서만 되는 것이 아니라 고객사 release 환경에서 살아남아야 해서, minify가 켜진 상태 기준으로도 배포 구성을 맞췄습니다."

---

## 최종 추천

포트폴리오에는 아래처럼 섞는 편이 좋다.

- 설계 개선형 2개
  - SDK 결과 반환 구조 표준화
  - Camera Lifecycle 자동 관리
- 알고리즘/품질 개선형 2개
  - 왜곡 이미지 전처리 파이프라인
  - FPS/실험 지표 정립
- 운영/현장 대응형 1개
  - 내부망 인증 또는 티켓 예외 처리
- 배포/플랫폼형 1개
  - 크로스 플랫폼 MVP 연동 또는 JNI/AAR 배포 안정화

이렇게 구성하면 "기획만 하는 사람"이 아니라 "문제 정의부터 코드, 운영, 배포까지 본 사람"으로 보인다.

## 포트폴리오에 넣을 때 피하면 좋은 표현

- "니즈를 충족하지 못함"
  - 너무 일반적이다. 어떤 검토가 막혔는지 쓰는 편이 낫다.
- "신뢰 확보"
  - 가능하면 "계약 성사", "파일럿 통과", "문의 감소"처럼 관찰 가능한 결과로 바꾼다.
- "포토샵 유사 기능"
  - 엔지니어링 설명으로는 약하다. 전처리 파이프라인으로 바꾼다.
- "안정적 확보"
  - 무엇이 얼마나 안정적인지 질문이 붙기 쉽다.

## 다음 단계 제안

다음 버전에서는 각 사례마다 아래 1줄씩만 추가하면 훨씬 강해진다.

- 사용 기술: `Camera2`, `OpenCV`, `Coroutine`, `Enum`, `LifecycleOwner`, `MethodChannel`
- 검증 기준: `문의량`, `크래시`, `인식 성공률`, `처리 시간`, `고객사 도입 여부`
- 내가 맡은 범위: `설계`, `SDK API 수정`, `앱 구현`, `가이드 문서 작성`, `현장 지원`
